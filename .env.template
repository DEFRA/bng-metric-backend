AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test
# Shared HMAC secret used to verify id_tokens the frontend forwards. MUST be set
# to the SAME value in the frontend and backend. Never commit a real secret here
# - replace this placeholder locally with a long random string (>= 16 chars).
AUTH_FORWARD_SECRET=replace-with-shared-secret
