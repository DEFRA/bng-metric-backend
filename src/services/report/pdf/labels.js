/**
 * The two document-side labels, in one place.
 *
 * They appear in the key-figures header, the map panel captions, the legend
 * and the habitat-page heading, and a report that called the same side
 * "Post-intervention" on one page and something else on another would read as
 * two different documents.
 */

const BASELINE = 'Baseline'
const POST_INTERVENTION = 'Post-intervention'

export { BASELINE, POST_INTERVENTION }
