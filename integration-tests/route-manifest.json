{
  "routes": [
    "GET /health",
    "GET /db-info",
    "GET /projects",
    "GET /projects/{id}",
    "POST /projects/new",
    "PATCH /projects/{id}",
    "POST /upload/initiate",
    "GET /upload/{uploadId}/status",
    "GET /users/{userId}/projects"
  ],
  "excluded": [
    {
      "route": "POST /baseline/validate/{uploadId}",
      "reason": "better-sqlite3 (used by the GeoPackage validator) is not fork-safe and crashes Vitest worker processes. The route is covered by unit tests (src/routes/baseline.test.js). Integration coverage to be restored once the native-module isolation issue is resolved."
    }
  ]
}
